# ── TravelWise Docker Compose ──────────────────────────────────────────
# 啟動方式：docker compose up --build
# 服務：redis | backend | frontend
#
# 本地存取：
#   前端  → http://localhost:3000
#   後端  → http://localhost:5001
#   Swagger UI → http://localhost:5001/api/docs

services:

  # ── Redis 快取（景點 / 天氣）──────────────────────────────────
  redis:
    image: redis:7-alpine
    container_name: travelwise_redis
    ports:
      - "6379:6379"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 3

  # ── Flask 後端 ────────────────────────────────────────────────
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: travelwise_backend
    ports:
      - "5001:5000"
    env_file:
      - ./backend/.env
    environment:
      # Redis 指向容器內的 redis service（覆蓋 .env 的 localhost 設定）
      REDIS_URL: redis://redis:6379/0
      # 注意：FLASK_TESTING 改由 backend/.env 控制（不在此寫死，讓每位開發者可個別調整）
      #   FLASK_TESTING=false → 正式驗證 Firebase ID Token，依 uid 隔離各使用者資料（預設）
      #   FLASK_TESTING=true  → 跳過驗證、所有人共用 test-user-001（純後端/單機開發用，會混資料）
    volumes:
      # 掛載 firebase service account（不 commit，本機需要此檔案）
      - ./backend/firebase_service_account.json:/app/firebase_service_account.json:ro
    depends_on:
      redis:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/api/trips", "-H", "Authorization: Bearer TEST_MODE"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 20s

  # ── Vue 前端（Nginx 靜態服務）────────────────────────────────
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        # Build 時注入後端 API URL
        # 瀏覽器直接打 API → 使用 localhost:5001（因為是從使用者瀏覽器發出）
        VITE_API_URL: http://localhost:5001
    container_name: travelwise_frontend
    ports:
      - "3000:80"
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:80/health"]
      interval: 15s
      timeout: 5s
      retries: 3


